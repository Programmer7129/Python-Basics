# If Else Statements

# Short Hand If Statements
a = 200
b = 35

if a > b: print("a is greater than b")

# Continue statement skips the remaining part of the loop and jumps back to the start
# Break statement breaks from the loop and comes out of the loop.

# While using for loop we can use pass if we are not passing any arguments to avoid any error
